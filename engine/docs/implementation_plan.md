# Implementation Plan & Change Process (v1.0.0)

1. Freeze API header and JSON schema.
2. Add `engine/VERSION` with `1.0.0`.
3. Add schema validation as part of CI.
4. Require reviewers to approve API/schema changes; record changes in changelog and increment version.
